import { generateMetadata } from '@/shared/lib/metadata';

export const metadata = generateMetadata('Work | Daniil Dev');

export default function Work() {
  return <main className="mt-14">My work in progress...</main>;
}
